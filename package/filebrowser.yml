server:
  port: 80
  database: "/var/lib/filebrowser/filebrowser.db"
  cacheDir: "/var/lib/filebrowser/tmp"
  #sources:
    # Do not use a root "/" directory or include the "/var" folder
    # - path: "/var/lib/filebrowser/sources/documentation"
    #   name: "Documentation"
    #   config:
    #     defaultEnabled: true

auth:
  adminUsername: admin
  methods:
      password:
        enabled: true
        minLength: 8
        signup: false

frontend:
  name: "Intranet"
  # description: ""
  # favicon: "/path/to/favicon.png"
  # loginIcon: "/path/to/login-icon.png"
  # loginButtonText: "Sign in"
  # oidcLoginButtonText: "Sign in with SSO"
  # externalLinks:
  #   - text: "Support"
  #     title: "Get support"
  #     url: "https://support.example.com"
